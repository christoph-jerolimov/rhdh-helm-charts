upstream:
  backstage:
    appConfig:
      # dangerouslyAllowSignInWithoutUserInCatalog: true
      auth:
        environment: production
        providers:
          github:
            production:
              # Requires a GitHub app and updated callback URL:
              # https://github.com/settings/apps
              clientId: TODO
              clientSecret: TODO
      # integrations:
      #   github:
      #     - host: github.com
      #       token: github_pat_TODO
